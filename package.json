{
  "name": "flutter-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "set -e && bash ./build.sh",
    "build": "bash ./build.sh"
  },
  "engines": {
    "node": "18.x"
  }
}
