{
  "name": "flutter-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "mkdir -p /tmp/flutter && cd /tmp/flutter && wget https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.29.1-stable.tar.xz && tar xf flutter_linux_3.29.1-stable.tar.xz && export PATH=\"$PATH:/tmp/flutter/flutter/bin\" && cd $INIT_CWD && flutter pub get && flutter build web --release",
    "build": "bash ./build.sh"
  },
  "engines": {
    "node": "18.x"
  }
}
